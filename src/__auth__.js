/**
 * 1. login === sign in ==> you are and old user . You already have the credential (email and password) to login.
 * 2. register === sign up === create account ==> You are a brand new user. you have never used the website before. you do not have any email, password, that have access. 
 * 3. 
 */